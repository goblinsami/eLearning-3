  <a class="text-muted mt-5 menu-toggle" id="" href=""><i id="open-btn" class="fas fa-chevron-circle-left"></i></i> Open</a>
<div id="sidebar-wrapper">
  <ul class="sidebar-nav">
    <li class="sidebar-brand">
      <i style="cursor: pointer" class="fas fa-user-graduate text-white menu-toggle"></i>
    </li>
        <% @courses.each do |course| %>
          <li class="menu-item">
            <%= link_to course, remote: true, data: {action: 'ajax:success->menu#replace'}  do %>
              <%= course.title %>
              <% end %>
          </li>
        <% end %>
  </ul>
</div>
